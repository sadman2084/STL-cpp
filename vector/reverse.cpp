#include<bits/stdc++.h>
using namespace std;
int main()
{
     vector<int> v;

    v.push_back(24);
    v.push_back(16);
    v.push_back(43);
    v.push_back(33);

    for(auto i=v.begin();i!=v.end();i++){
        
    }
}